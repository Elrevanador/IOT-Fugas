# Railway-specific npm configuration
# Ignore warnings and focus on actual errors
ignore-scripts=false
audit=false
fund=false
# Use production mode explicitly
production=true
# Don't install dev dependencies in production
omit=dev
# More verbose logging for debugging
loglevel=warn